######################################################################
# makefile_DelayReq_x64Darwin16clang8.0#
# (c) Copyright, Real-Time Innovations, 2012.  All rights reserved.
# No duplications, whole or partial, manual or electronic, may be made
# without express written permission.  Any such copies, or
# revisions thereof, must display this notice unaltered.
# This code contains trade secrets of Real-Time Innovations, Inc.
#
#
# This makefile was automatically generated by rtiddsgen.
#
# To compile, type:
# 	gmake -f makefile_DelayReq_x64Darwin16clang8.0#
# Note: This makefile is only meant to build our example applications and
#       may require alterations to build on your system.
#
# Make sure that javac and java are in your path.
######################################################################

SOURCE_DIR = 

TARGET_ARCH=x64Darwin16clang8.0

# If undefined in the environment default NDDSHOME to install dir
ifndef NDDSHOME
NDDSHOME := /Applications/rti_connext_dds-5.3.0
endif

JAVA_PATH = java
JAVAC_PATH = javac

JAVA_SOURCES = $(SOURCE_DIR)./DelayRequest.java $(SOURCE_DIR)./DelayRequestSeq.java $(SOURCE_DIR)./DelayRequestTypeSupport.java $(SOURCE_DIR)./DelayRequestTypeCode.java $(SOURCE_DIR)./DelayRequestDataReader.java $(SOURCE_DIR)./DelayRequestDataWriter.java $(SOURCE_DIR)./DelayRequestSubscriber.java $(SOURCE_DIR)./DelayRequestPublisher.java
CLASS_FILES  = $(JAVA_SOURCES:%.java=%.class)

RTI_CLASSPATH := $(NDDSHOME)/lib/java/nddsjava.jar

%.class : %.java
	$(JAVAC_PATH)  -classpath ".:$(RTI_CLASSPATH)" $<

all: $(CLASS_FILES)
 
#
# Convenient way to run the java programs
#

export DYLD_LIBRARY_PATH := $(NDDSHOME)/lib/x64Darwin16clang8.0:/usr/lib/lwp:$(DYLD_LIBRARY_PATH)

DelayRequestPublisher: ./DelayRequestPublisher.class
	$(JAVA_PATH) -d64 -Djava.library.path=$(NDDSHOME)/lib/x64Darwin16clang8.0 -classpath ".:$(RTI_CLASSPATH)" DelayRequestPublisher $(ARGS)
 
DelayRequestSubscriber: ./DelayRequestSubscriber.class
	$(JAVA_PATH) -d64 -Djava.library.path=$(NDDSHOME)/lib/x64Darwin16clang8.0 -classpath ".:$(RTI_CLASSPATH)" DelayRequestSubscriber $(ARGS)
